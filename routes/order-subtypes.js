const express = require("express");
const router = express.Router();

const orderSubTypes = require("../services/orderSubtypes");

// router.get("/", orderSubTypes.getOrderSubtypes);

module.exports = router;
